import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'PromptForge AI - AI-Powered Prompt Enhancement',
  description: 'Enhance your AI prompts with advanced optimization techniques powered by NVIDIA AI',
  keywords: 'AI, prompt engineering, optimization, NVIDIA, machine learning',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
